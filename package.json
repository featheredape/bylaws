{
  "name": "ssi-bylaw-checker",
  "version": "2.0.0",
  "description": "Salt Spring Island Bylaw Compliance Checker",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public",
    "dev:ai": "npx wrangler pages dev public --ai",
    "deploy": "npx wrangler pages deploy public --project-name ssi-bylaw-checker",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
